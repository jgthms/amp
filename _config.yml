# Site settings
title: AMP Test
email: your-email@domain.com
description: > # this means to ignore newlines until "baseurl:"
  This is a test for AMP: the Accelerated Mobile Pages project.
baseurl: "/amp"
exclude: ["_scripts"]

# Build settings
markdown: kramdown
sass:
  style: compressed
